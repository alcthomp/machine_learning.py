import json
from pprint import pprint
import sys
import os

trafficList = ["srcIP","srcPort","dstIP","dstPort"]

main()
def main(): 
	with open

def allFiles(): 
	global fileCount
	dirs = [d for d in os.listdir(FILEPATH) if os.path.isdir(os.path.join(FILEPATH,d))]
	
	for directory in dirs:
		if re.search(r'(\d{3})7-(\d{2})',directory)
			for FILE in os.listdir(os.path.join(FILEPATH,directory)):
				if "conn" in file:
					FILELIST.append(FILEPATH + directory + "/" + FILE)
					fileCount += 1

for FILE in FILELIST:
	with open('data.json') as data_file:    
		data = json.load(data_file)
pprint(data)

 data["maps"][0]["id"]
 data["masks"]["id"]
 data["om_points"]